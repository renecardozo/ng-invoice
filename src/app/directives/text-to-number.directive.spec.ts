import { TextToNumberDirective } from './text-to-number.directive';

describe('TextToNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToNumberDirective();
    expect(directive).toBeTruthy();
  });
});
